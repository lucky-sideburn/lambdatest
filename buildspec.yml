version: 0.2

phases:
  build:
    commands:
      - mkdir build
      - pip install -r src/hello-world/requirements.txt --target src/hello-world
      - zip -r build/foo.zip src/hello-world
artifacts:
  files:
    - 'build/*.zip'
  name: foo-$(date +%Y-%m-%d) 
